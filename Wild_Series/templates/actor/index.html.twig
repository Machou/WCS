{% extends 'base.html.twig' %}

{% block title %}Tous les Acteurs{% endblock %}

{% block body %}
    <h1>Les Acteurs</h1>

    <div class="my-5">
        {% for actor in actors %}
        <div class="actors-box">
            <h3>{{ actor.name }}</h3>

            <p>
                <a href="{{ path("actor_show", { id: actor.id }) }}" class="btn btn-primary">Voir la Fiche</a>
                {% if is_granted('ROLE_ADMIN') %}<a href="{{ path('actor_edit', {id: actor.id}) }}" class="btn btn-warning">Modifier</a>{% endif %}
            </p>
        </div>
        {% else %}
            Aucun Acteur trouvé.
        {% endfor %}
    </div>

    <p><a href="{{ path('index') }}" class="btn btn-info">Retourner à l'Accueil</a></p>
{% endblock %}