{% extends 'base.html.twig' %}

{% block title %}Modifier un acteur{% endblock %}

{% block body %}
    <h1>Modifier un acteur</h1>

    <div>
		{{ include('actor/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('actor_index') }}" class="btn btn-info mt-3 text-white">Retourner à la liste des acteurs</a>

		{{ include('actor/_delete_form.html.twig') }}
    </div>
{% endblock %}