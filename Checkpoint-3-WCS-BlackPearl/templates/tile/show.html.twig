{% extends 'base.html.twig' %}

{% block title %}Tile{% endblock %}

{% block body %}
    <h1>Tile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tile.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ tile.type }}</td>
            </tr>
            <tr>
                <th>CoordX</th>
                <td>{{ tile.coordX }}</td>
            </tr>
            <tr>
                <th>CoordY</th>
                <td>{{ tile.coordY }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tile.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tile_index') }}">back to list</a>

    <a href="{{ path('tile_edit', {'id': tile.id}) }}">edit</a>

    {{ include('tile/_delete_form.html.twig') }}
{% endblock %}
