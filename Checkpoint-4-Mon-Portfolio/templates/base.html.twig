<!DOCTYPE html>
<html>
<head lang="fr" dir="ltr">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>{% block title %}Bienvenue !{% endblock %} • Portfolio de Charlie</title>

    {% block stylesheets %}{{ encore_entry_link_tags('app') }}{% endblock %}
</head>

<body>
<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('index') }}">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item{{ app.request.get('_route') == 'projets_index' ? ' active' }}"><a class="nav-link" href="{{ path('projets_index') }}">Mes Projets</a></li>
            <li class="nav-item{{ app.request.get('_route') == 'services' ? ' active' }}"><a class="nav-link" href="{{ path('services') }}">Mes Services</a></li>
            <li class="nav-item{{ app.request.get('_route') == 'contact' ? ' active' }}"><a class="nav-link" href="{{ path('contact') }}">Me contacter</a></li>
            {% if app.user %}
            <li class="nav-item{{ app.request.get('_route') == 'app_logout' ? ' active' }}"><a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a></li>
            {% else %}
            <li class="nav-item{{ app.request.get('_route') == 'app_login' ? ' active' }}"><a class="nav-link" href="{{ path('app_login') }}">Se connecter</a></li>
            <li class="nav-item{{ app.request.get('_route') == 'app_register' ? ' active' }}"><a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a></li>
            {% endif %}
        </ul>
        </div>
    </nav>
</header>

<div class="container pt-5">
    {% include '_flash.html.twig' %}
    {% block body %}{% endblock %}
</div>

{% block javascripts %}{{ encore_entry_script_tags('app') }}{% endblock %}
</body>
</html>